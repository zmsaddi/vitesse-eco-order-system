# القواعد التجارية — Business Rules

> **رقم العنصر**: #09 | **المحور**: ب | **الحالة**: مواصفات نهائية
> **المصدر الكنسي**: هذا الملف هو المرجع الوحيد لجميع القواعد التجارية
> **الدقة المالية**: 0.01€ — كل الأرقام TTC (قرار H1)

---

## قواعد التسعير

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-01 | **حد أدنى مطلق** | لا يمكن لأي دور البيع بأقل من buy_price (سعر الشراء) |
| BR-02 | **حد أدنى للبائع** | البائع لا يبيع بأقل من sell_price. PM/GM/Manager يمكنهم بين buy_price و sell_price |
| BR-03 | **sell_price ≥ buy_price** | عند تعديل سعر البيع يجب أن يكون ≥ سعر الشراء |

## قواعد المخزون

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-04 | **منع البيع الزائد** | لا يمكن بيع كمية أكبر من المخزون المتاح. FOR UPDATE lock ذري |
| BR-05 | **حجز فوري** | المخزون يُحجز لحظة إنشاء الطلب (وليس عند التسليم) |
| BR-06 | **المنتجات لا تُحذف** | المنتجات تُعطّل فقط (active=false). لا حذف نهائي أبداً (قرار H6) |

## قواعد الدفع

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-07 | **كاش/بنك** | طريقة الدفع كاش أو بنك = الدفع الكامل مطلوب عند التسليم |
| BR-08 | **آجل** | طريقة الدفع آجل = دفعة مقدمة اختيارية (0 إلى total) |
| BR-09 | **منع الدفع الزائد** | المبلغ المحصّل ≤ المتبقي (ضمن تسامح 0.01€) |
| BR-10 | **تحصيل على المؤكد فقط** | لا يمكن تحصيل دفعة على طلب غير مؤكد أو ملغي |

## قواعد العملاء

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-11 | **إنشاء تلقائي** | إذا العميل غير موجود عند البيع → يُنشأ تلقائياً |
| BR-12 | **التفرد** | (اسم + هاتف) أو (اسم + إيميل) = مفتاح التعريف. عند التكرار → عرض المرشحين |
| BR-13 | **حماية الحذف** | لا يمكن حذف عميل له طلبات غير ملغاة |

## قواعد الموردين

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-14 | **لا حذف للموردين (D-76 + D-04)** | `suppliers` تُعطَّل بـ `active=false` فقط، لا hard delete. RESTRICT FKs من purchases/supplier_payments تمنع أي DELETE عرضي. |

## قواعد الإلغاء

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-15 | **الإلغاء مسموح دائماً** | لا منع للإلغاء حتى لو العمولة مصروفة (قرار C2 — BR-17 القديم أُلغي) |
| BR-16 | **صلاحيات الإلغاء** | PM/GM: أي حالة. Manager: محجوز فقط. Seller: خاصتي المحجوزة. Driver/Stock Keeper: لا |
| BR-17 | **سبب إلزامي** | كل إلغاء يتطلب سبب نصي غير فارغ |
| BR-18 | **شاشة إلغاء C1** | 3 خيارات إلزامية في جدول `cancellations`: (1) `return_to_stock` (bool) — إعادة الكمية إلى stock؟ (2) `seller_bonus_action ∈ {keep, cancel_unpaid, cancel_as_debt}` — keep=إبقاء مع status=retained؛ cancel_unpaid=حذف صف bonus (للعمولات غير المصروفة)؛ cancel_as_debt=تسوية سالبة (للعمولات المصروفة). (3) `driver_bonus_action` — نفس المنطق. عند عدم وجود عمولات: القيمة `cancel_unpaid` افتراضياً بلا تعطيل. كل ذلك داخل transaction واحدة مع 8 invariants محفوظة في الاختبارات. |
| BR-19 | **عمولة مصروفة → دين** | عند اختيار `cancel_as_debt` لعمولة settled → يُدرَج صف **settlement سالب** (`amount = -total_bonus`) يُنتج عنه خصم من الدفعة التالية لنفس المستلم. الدين قابل للتجديد (لا تسقط بفترة). |
| BR-20 | **سجل الإلغاء** | كل إلغاء يُسجل في جدول cancellations مع كل القرارات والحالات السابقة |

## قواعد التوصيل

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-21 | **VIN حسب الفئة** | VIN مطلوب لكل `order_item` في الفئات المحددة في `settings.vin_required_categories` (JSON array في text). الافتراضي: `["دراجات كهربائية","دراجات عادية"]`. التحقق في Zod عند POST + عند تحديث الحالة إلى `تم التوصيل`. |
| BR-22 | **VIN لكل صنف** | كل order_item له حقل vin مستقل. طلب بدراجتين = 2 VINs |
| BR-23 | **تعيين السائق اختياري** | يمكن تأكيد التسليم بدون سائق معيّن — المؤكِد = السائق ضمنياً (قرار M10) |
| BR-24 | **إلغاء توصيل "جاري" ≠ إلغاء بيع** | التوصيل يعود "قيد الانتظار"، الطلب يبقى "محجوز". سبب إلزامي (قرار H2) |

## قواعد الطلبات المتعددة

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-25 | **طلب متعدد الأصناف** | الطلب الواحد يحتوي عدة أصناف (دراجة + إكسسوار + قطع غيار) |
| BR-26 | **إجمالي الطلب** | total_amount = مجموع line_total لكل الأصناف بعد الخصومات |
| BR-27 | **تعديل المحجوز** | يمكن إضافة/حذف/تعديل أصناف في طلب محجوز. تحذير إذا الدفعة المقدمة تتأثر (قرار H3) |
| BR-28 | **تعديل المؤكد ممنوع** | لا تعديل على طلب مؤكد — إلغاء فقط |

## قواعد العمولات

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-29 | **عمولة حسب فئة المنتج** | كل فئة لها معدل عمولة مختلف (product_commission_rules) |
| BR-30 | **عمولة لكل صنف** | تُحسب عمولة مستقلة لكل order_item عند التسليم |
| BR-31 | **توقيت الحساب** | العمولة تُحسب لحظة تأكيد التسليم فقط — ليس عند إنشاء الطلب |
| BR-32 | **معدل snapshot وقت الإنشاء** | المعدل المُطبَّق هو المخزَّن في `order_items.commission_rule_snapshot JSONB` لحظة إنشاء الـ order_item. تغييرات لاحقة في `user_bonus_rates` أو `product_commission_rules` أو settings لا تؤثر على الطلبات السابقة. يحمي حقوق البائع المُكتسبة. قرار D-17 يلغي M14. |
| BR-33 | **أهلية قابلة للتكوين** | كل دور يمكن تكوين أهليته للعمولة في الإعدادات (قرار M1) |
| BR-34 | **CHECK على المعدلات** | seller_percentage: 0-100. seller_fixed ≥ 0. driver_fixed ≥ 0 (قرار M5) |

## قواعد الهدايا

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-35 | **تحديد الهدايا** | PM/GM فقط يحددون المنتجات القابلة للإهداء والكمية (gift_pool) |
| BR-36 | **إضافة هدية** | البائع/Manager يضيف هدية من المتاح في gift_pool. is_gift=true, discount=100% |
| BR-37 | **الفاتورة** | الهدية تظهر بسعر البيع + خصم 100% = 0€ |
| BR-38 | **المخزون** | المخزون ينقص عند حجز الهدية (مثل أي صنف) |
| BR-39 | **التكلفة** | تكلفة الهدية (buy_price) تُخصم من الأرباح |

## قواعد الخصومات

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-40 | **أنواع الخصم** | نسبة مئوية أو مبلغ ثابت — على مستوى الصنف (order_item) |
| BR-41 | **حدود الخصم** | PM/GM بلا سقف. Manager: حد أقصى **15%** من `unit_price`. Seller: **5%**. القيم قابلة للتعديل في `settings.max_discount_manager_pct` و `settings.max_discount_seller_pct`. التحقق في Zod refine وفي middleware الـ API قبل الحفظ. |
| BR-42 | **سبب الخصم** | حقل اختياري لتوثيق سبب الخصم |

## قواعد المستخدمين

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-43 | **لا حذف نهائي** | المستخدمون يُعطّلون فقط (active=false). لا DELETE أبداً |
| BR-44 | **عمولات المعطّل** | المستخدم المعطّل تبقى عمولاته غير المصروفة قابلة للتسوية (قرار M8) |
| BR-45 | **إعادة التفعيل** | يمكن إعادة تفعيل مستخدم معطّل |

## قواعد مالية عامة

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-46 | **كل الأرقام TTC** | كل المبالغ في النظام TTC. TVA تُحسب فقط عند توليد الفاتورة (قرار H1) |
| BR-47 | **TVA من الإعدادات** | نسبة TVA موحدة من settings — تُستخدم للفاتورة فقط (قرار M2) |
| BR-48 | **حذف ناعم دائماً** | لا حذف نهائي لطلبات/توصيلات/فواتير/منتجات — تغيير حالة فقط (قرار H5) |
| BR-49 | **الاسم cached، لا تحديث cascade** | (D-20 يُلغي H4 القديم). الـ FK هو `*_id` (مصدر truth). الأعمدة `*_name_cached` في السجلات التاريخية **لا تُحدَّث** عند إعادة تسمية الكيان الأصلي — السجل التاريخي يعرض الاسم وقت الإنشاء (صحيح محاسبياً). Dashboards الحية تقرأ الاسم الحالي عبر JOIN على `*_id`. |
| BR-50 | **الدقة المالية** | NUMERIC(19,2) لكل المبالغ. التسامح: 0.01€ |
| BR-51 | **المنطقة الزمنية** | Europe/Paris لكل العمليات (قرار L3) |

## قواعد الصناديق

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-52 | **هيكل هرمي** | GM (كاش+بنك) → صناديق المدراء → عهدات السائقين |
| BR-53 | **تحديث ذري** | كل حركة مالية تُحدّث الرصيد ذرياً (withTx) |
| BR-54 | **تسوية يومية** | المدير يُدخل الرصيد الفعلي → النظام يقارن بالمحسوب |
| BR-55 | **كل عملية → حركة** | كل تحصيل/دفع/مصروف/تسوية/مكافأة → حركة تلقائية في treasury_movements |
| BR-55b | **سقف عهدة السائق (D-44)** | `/api/orders/[id]/collect` و `/api/clients/[id]/collect`: إذا `driver.role === 'driver'` و `driver_custody.balance + newAmount > settings.driver_custody_cap_eur` → **يُرفَض** بـ `409 CUSTODY_CAP_EXCEEDED`. السائق ملزم بتسليم الأموال لمديره أولاً. Override يدوي (PM فقط) عبر header `X-Force-Collect: true` مع تسجيل في `activity_log`. |

## قواعد المشتريات

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-56 | **لا إلغاء** | المشتريات تُحذف فقط (لا إلغاء). عند الحذف: شاشة C5 (قرار M7) |
| BR-57 | **حذف مشتريات** | سؤال "هل استُرد المبلغ؟" نعم→ inflow بالصندوق. لا→ رصيد دائن للمورد (قرار C5) |
| BR-58 | **متوسط مرجح** | سعر الشراء يُحدّث بالمتوسط المرجح عند كل عملية شراء |

## قواعد توزيع الأرباح

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-59 | **مرة واحدة لكل فترة** | كل فترة تُوزّع مرة واحدة فقط (قرار L5) |
| BR-60 | **النسب = 100%** | مجموع نسب المستلمين يجب أن يساوي 100% (±0.01%) |
| BR-61 | **created_by إلزامي** | كل توزيع يُسجل من أنشأه (قرار H8) |

## قواعد النسخ الاحتياطي

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-62 | **نسخ احتياطي يدوي** | زر في الإعدادات لـ PM/GM — تنزيل نسخة أو استعادة من ملف (قرار C4) |

## قواعد الفواتير

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-63 | **توليد تلقائي** | الفاتورة تُنشأ تلقائياً عند تأكيد التسليم |
| BR-64 | **ترقيم شهري** | `FAC-YYYY-MM-NNNN` — ذري عبر invoice_sequence (D-01) |
| BR-65 | **إلغاء الفاتورة ناعم** | عند إلغاء الطلب → `UPDATE invoices SET status='ملغي'`. لا hard-delete (D-04 + D-09). لا مفتاح `invoice_mode` — هذا السلوك ثابت. |
| BR-66 | **لغة الفاتورة** | الفاتورة بالفرنسية (متطلب قانوني). الكتالوج بـ 3 لغات (AR/EN/FR) |

## قواعد الترقيم

| # | القاعدة | التفصيل |
|---|---------|---------|
| BR-67 | **أنماط الترقيم** | `ORD-YYYYMMDD-NNNNN` (طلبات)، `PU-YYYYMMDD-NNNNN` (مشتريات)، `DL-YYYYMMDD-NNNNN` (توصيل)، `FAC-YYYY-MM-NNNN` (فواتير — D-01) |
| BR-68 | **5 أرقام** | كافي — ref_code بـ 5 أرقام (قرار L4) |
